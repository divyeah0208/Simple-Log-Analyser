Log Analyzer (Python)

A basic Python log analysis tool that reads a log file and provides:

Total number of lines

Number of failed login attempts

Number of unique IP addresses

Top 5 most frequent IPs

This tool works with any text-based log file (e.g., auth logs, web server logs, custom logs).
